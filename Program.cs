static string[] FilterStrings(string[] array)
{
    int count = CountFilteredStrings(array);
    string[] filteredArray = new string[count];
    int index = 0;
    return filteredArray;
}

static int CountFilteredStrings(string[] array)
{
    int count = 0;
    return count;
}

static bool IsStringValid(string str)
{


    return false;
}